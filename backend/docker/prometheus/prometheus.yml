global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "crm_backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["nginx:80"]

rule_files:
  - alert-rules.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093